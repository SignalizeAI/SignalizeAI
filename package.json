{
  "name": "signalize",
  "version": "1.0.0",
  "description": "Cursor for sales pages 🚀",
  "main": "background.js",
  "scripts": {
    "build": "esbuild src/sidepanel-entry.js --bundle --format=iife --outfile=extension/supabase.bundle.js",
    "watch": "esbuild src/sidepanel-entry.js --bundle --format=iife --outfile=extension/supabase.bundle.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.78.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
